    $(function() {

    // Create the dialog and make it hidden initially
    $('#cover-letter-dialog').dialog({ autoOpen: false });

    // When the link is clicked, open the dialog
    $('#cover-letter').click(function() {
        $('#cover-letter-dialog').dialog('open');
        return false;
    });

});

    $(function() {

    // Create the dialog and make it hidden initially
    $('#about-dialog').dialog({ autoOpen: false });

    // When the link is clicked, open the dialog
    $('#about').click(function() {
        $('#about-dialog').dialog('open');
        return false;
    });

});

    $(function() {

    // Create the dialog and make it hidden initially
    $('#resume-dialog').dialog({ autoOpen: false });

    // When the link is clicked, open the dialog
    $('#resume').click(function() {
        $('#resume-dialog').dialog('open');
        return false;
    });

});

    $(function() {

    // Create the dialog and make it hidden initially
    $('#contact-dialog').dialog({ autoOpen: false });

    // When the link is clicked, open the dialog
    $('#contact').click(function() {
        $('#contact-dialog').dialog('open');
        return false;
    });

});